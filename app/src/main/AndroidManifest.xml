<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rub_a_dub_grub3">

    <uses-permission android:name="android.permission.INTERNET"/> <!--  These three permissions allow app to access internet and for the boolean function of connection check -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustNothing">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectionActivity" />
        <activity android:name=".LunchMenuActivity" />
        <activity android:name=".DinnerMenuActivity" />
        <activity android:name=".DrinksMenuActivity" />
        <activity android:name=".BreakfastMenuActivity" />
        <activity android:name=".CheckoutActivity" />
    </application>
</manifest>